---
/**
 * Home Page - Loui Jay Naquines Portfolio
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import SEO from '../components/SEO.astro';
import StructuredData from '../components/StructuredData.astro';
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import profileImage from '../assets/profile1.jpg';
import photo from '../assets/profile.jpg';
import photo2 from '../assets/pic2.jpg';
import photo3 from '../assets/pic3.jpg';
import photo4 from '../assets/pic4.jpg';
import photo5 from '../assets/pic1.jpg';
import photo6 from '../assets/pic5.jpg';
import Chatbot from '../components/Chatbot.astro';


const allProjects = await getCollection('projects');
const featuredProjects = allProjects
  .filter(p => p.data.featured === true)
  .sort((a, b) => (a.data.order || 999) - (b.data.order || 999))
  .slice(0, 4);

const allProjectsSorted = allProjects
  .sort((a, b) => (a.data.order || 999) - (b.data.order || 999));

const techStack = {
  Frontend: [
    { name: "React",       icon: "https://cdn.simpleicons.org/react/61DAFB" },
    { name: "Astro",       icon: "https://cdn.simpleicons.org/astro/FF5D01" },
    { name: "TypeScript",  icon: "https://cdn.simpleicons.org/typescript/3178C6" },
    { name: "Tailwind CSS", icon: "https://cdn.simpleicons.org/tailwindcss/06B6D4" },
    { name: "Next.js",      icon: "https://cdn.simpleicons.org/nextdotjs/000000" },
  ],
  Backend: [
    { name: "Node.js",   icon: "https://cdn.simpleicons.org/nodedotjs/339933" },
    { name: "Express",   icon: "https://cdn.simpleicons.org/express/000000" },
    { name: "PHP",       icon: "https://cdn.simpleicons.org/php/777BB4" },
    { name: "REST APIs", icon: "https://cdn.simpleicons.org/fastapi/05998B" },
  ],
  Database: [
    { name: "PostgreSQL", icon: "https://cdn.simpleicons.org/postgresql/4169E1" },
    { name: "MySQL",      icon: "https://cdn.simpleicons.org/mysql/4479A1" },
    { name: "MongoDB",    icon: "https://cdn.simpleicons.org/mongodb/47A248" },
    { name: "Supabase",   icon: "https://cdn.simpleicons.org/supabase/3ECF8E" },
  ],
  Tools: [
    { name: "Git",    icon: "https://cdn.simpleicons.org/git/F05032" },
    { name: "Docker", icon: "https://cdn.simpleicons.org/docker/2496ED" },
    { name: "Figma",  icon: "https://cdn.simpleicons.org/figma/F24E1E" },
    { name: "Vercel", icon: "https://cdn.simpleicons.org/vercel/000000" },
  ],
};

const galleryImages = [
  { src: photo,  alt: "Photo 1" },
  { src: photo2, alt: "Photo 2" },
  { src: photo3, alt: "Photo 3" },
  { src: photo4, alt: "Photo 4" },
  { src: photo5, alt: "Photo 5" },
  { src: photo6, alt: "Photo 6" },
];
---

<BaseLayout>
  <SEO
    slot="head"
    title="Loui Jay Naquines — Full Stack Developer"
    description="Full stack developer and CS student based in Mandaue City, Cebu."
    type="website"
    noSuffix
  />
  <StructuredData type="WebSite" />
  <StructuredData type="Person" />

  <main class="page">

    <!-- ── PAGE LOADER ── -->
    <div id="page-loader" class="page-loader">
      <div class="loader-content">
        <div class="loader-logo">LN</div>
        <div class="loader-bar"><div class="loader-bar-fill"></div></div>
      </div>
    </div>

    <!-- ── PROFILE HEADER ── -->
    <header class="profile-header reveal reveal-up">
      <div class="profile-header-inner">
        <Image src={profileImage} alt="Loui Jay Naquines" class="avatar" />
        <div class="profile-info">
          <div class="profile-name-row">
            <h1 class="profile-name">Loui Jay Naquines</h1>
          </div>
          <p class="profile-location">Mandaue City, Cebu</p>
          <p class="profile-role">Full Stack Developer · IT Student</p>
          <div class="profile-actions">
            <a href="/resume.pdf" download class="btn-primary">⬇ Download Resume</a>
            <button id="open-schedule-modal" class="btn-secondary btn-schedule-trigger">📅 Schedule a Call</button>
          </div>
        </div>
      </div>
    </header>

    <!-- ── MAIN GRID ── -->
    <div class="main-grid">

      <!-- LEFT COLUMN -->
      <div class="col-left">

        <!-- ABOUT -->
        <section id="about" class="card reveal reveal-up" style="scroll-margin-top:5rem; animation-delay: 0.1s">
          <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>About</h2>
          <p>
            I'm a Information Technology student passionate about building end-to-end web applications. I don't just ship features — I think about architecture, trade-offs, and long-term maintainability.
          </p>
          <p>
            Currently looking for a junior developer role where I can contribute to real products and grow alongside experienced engineers. Based in Cebu, open to remote.
          </p>
        </section>

        <!-- TECH STACK -->
        <section id="stack" class="card reveal reveal-up" style="scroll-margin-top:5rem; animation-delay:0.15s">
          <div class="card-title-row">
            <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 2v2M12 20v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M2 12h2M20 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>Tech Stack</h2>
            <button class="view-all" id="open-stack-modal">View All &rsaquo;</button>
          </div>
          {Object.entries(techStack).map(([category, tools]) => (
            <div class="stack-group">
              <h3 class="stack-category">{category}</h3>
              <div class="stack-tags">
                {tools.map(tool => (
                  <span class="tag">
                    <img src={tool.icon} alt={tool.name} class="tag-icon" width="13" height="13" loading="lazy" />
                    {tool.name}
                  </span>
                ))}
              </div>
            </div>
          ))}
        </section>

        <!-- RECENT PROJECTS -->
        <section class="card reveal reveal-up" style="animation-delay:0.2s">
          <div class="card-title-row">
            <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7a2 2 0 0 1 2-2h4l2 2h8a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></svg>Recent Projects</h2>
            <button class="view-all" id="open-projects-modal">View All &rsaquo;</button>
          </div>
          <div class="projects-grid">
            {featuredProjects.length > 0 ? featuredProjects.map(project => (
              <a href={`/projects/${project.id}`} class="project-card">
                <span class="project-role">{project.data.role}</span>
                <h3 class="project-title">{project.data.title}</h3>
                <p class="project-desc">{project.data.outcomeSummary}</p>
              </a>
            )) : (
              <>
                <div class="project-card">
                  <span class="project-role">Full Stack</span>
                  <h3 class="project-title">Project One</h3>
                  <p class="project-desc">Replace with your project description.</p>
                </div>
                <div class="project-card">
                  <span class="project-role">Frontend</span>
                  <h3 class="project-title">Project Two</h3>
                  <p class="project-desc">Replace with your project description.</p>
                </div>
                <div class="project-card">
                  <span class="project-role">Backend</span>
                  <h3 class="project-title">Project Three</h3>
                  <p class="project-desc">Replace with your project description.</p>
                </div>
                <div class="project-card">
                  <span class="project-role">Full Stack</span>
                  <h3 class="project-title">Project Four</h3>
                  <p class="project-desc">Replace with your project description.</p>
                </div>
              </>
            )}
          </div>
        </section>

        <!-- GALLERY -->
        <section class="card reveal reveal-up" style="animation-delay:0.25s">
          <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="M21 15l-5-5L5 21"/></svg>Gallery</h2>
          <p class="card-sub">Some photos of me.</p>
          <div class="gallery-grid">
            {galleryImages.map((img, i) => (
              <div class="gallery-item" data-index={i}>
                {img.src ? (
                  <img src={img.src.src ?? img.src} alt={img.alt} class="gallery-img" loading="lazy" />
                ) : (
                  <div class="gallery-placeholder"><span>📷</span></div>
                )}
              </div>
            ))}
          </div>
        </section>

      </div>

      <!-- RIGHT COLUMN -->
      <div class="col-right">

        <!-- EXPERIENCE -->
        <section class="card reveal reveal-left" style="animation-delay:0.1s">
          <div class="card-title-row">
            <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>Experience</h2>
            <button class="view-all" id="open-experience-modal">View All &rsaquo;</button>
          </div>
          <div class="timeline">
            <div class="timeline-item">
              <div class="timeline-dot active"></div>
              <div class="timeline-body">
                <span class="timeline-year">2024 — Present</span>
                <strong>Freelance Full Stack Developer</strong>
                <p>Building and deploying web solutions for local clients.</p>
              </div>
            </div>
            <div class="timeline-item">
              <div class="timeline-dot"></div>
              <div class="timeline-body">
                <span class="timeline-year">2023 — Present</span>
                <strong>IT Student</strong>
                <p>Studying algorithms, system design, and software engineering.</p>
              </div>
            </div>
          </div>
        </section>

        <!-- GOALS -->
        <section class="card reveal reveal-left" style="animation-delay:0.15s">
          <h2 class="card-title"><svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>Goals</h2>
          <div class="goals-list">
            <div class="goal-item">
              <strong>Land My First Dev Role</strong>
              <p>Looking for a junior position to contribute, learn, and grow fast.</p>
            </div>
            <div class="goal-item">
              <strong>Ship a SaaS Product</strong>
              <p>Take an idea from zero to paying users — owning the full stack.</p>
            </div>
            <div class="goal-item">
              <strong>Go Deeper on System Design</strong>
              <p>Understand how systems scale, where they break, and how to build for longevity.</p>
            </div>
            <div class="goal-item">
              <strong>Build in Public</strong>
              <p>Document projects and decisions openly to track growth and help others.</p>
            </div>
          </div>
        </section>

        <!-- CONTACT & SOCIALS -->
        <section id="contact" class="card reveal reveal-left" style="scroll-margin-top:5rem; animation-delay:0.2s">
          <h2 class="card-title">Contact & Socials</h2>

          <h3 class="contact-group-title">Contact</h3>
          <ul class="contact-list">
            <li>
              <span class="contact-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.8 19.8 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2.08 4.18 2 2 0 0 1 4.06 2h3a2 2 0 0 1 2 1.72c.12.89.33 1.76.63 2.6a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.48-1.15a2 2 0 0 1 2.11-.45c.84.3 1.71.51 2.6.63A2 2 0 0 1 22 16.92z"/></svg>
              </span>
              <div>
                <span class="contact-label">Phone</span>
                <a href="tel:+639XXXXXXXXX" class="contact-value">+63 9XX XXX XXXX</a>
              </div>
            </li>
            <li>
              <span class="contact-icon">
                <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" viewBox="0 0 24 24"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-10 7L2 7"/></svg>
              </span>
              <div>
                <span class="contact-label">Email</span>
                <a href="mailto:louijaynaquines@email.com" class="contact-value">louijaynaquines@email.com</a>
              </div>
            </li>
            <li>
              <span class="contact-icon">
                <img src="https://cdn.simpleicons.org/messenger/0084FF" width="15" height="15" alt="Messenger" />
              </span>
              <div>
                <span class="contact-label">Messenger</span>
                <a href="https://m.me/yourusername" target="_blank" rel="noopener" class="contact-value">m.me/yourusername</a>
              </div>
            </li>
          </ul>

          <ul class="contact-list" style="margin-top:1.5rem">
            <li>
              <span class="contact-icon">
                <img src="https://cdn.simpleicons.org/facebook/1877F2" width="15" height="15" alt="Facebook" />
              </span>
              <div>
                <span class="contact-label">Facebook</span>
                <a href="https://facebook.com/yourusername" target="_blank" rel="noopener" class="contact-value">facebook.com/yourusername</a>
              </div>
            </li>
            <li>
              <span class="contact-icon">
                <img src="https://cdn.simpleicons.org/instagram/E4405F" width="15" height="15" alt="Instagram" />
              </span>
              <div>
                <span class="contact-label">Instagram</span>
                <a href="https://instagram.com/yourusername" target="_blank" rel="noopener" class="contact-value">@yourusername</a>
              </div>
            </li>
            <li>
              <span class="contact-icon">
                <img src="https://cdn.simpleicons.org/linkedin/0A66C2" width="15" height="15" alt="LinkedIn" />
              </span>
              <div>
                <span class="contact-label">LinkedIn</span>
                <a href="https://linkedin.com/in/yourusername" target="_blank" rel="noopener" class="contact-value">linkedin.com/in/yourusername</a>
              </div>
            </li>
            <li>
              <span class="contact-icon">
                <img src="https://cdn.simpleicons.org/github/000000" width="15" height="15" alt="GitHub" />
              </span>
              <div>
                <span class="contact-label">GitHub</span>
                <a href="https://github.com/yourusername" target="_blank" rel="noopener" class="contact-value">github.com/yourusername</a>
              </div>
            </li>
          </ul>
        </section>

      </div>
    </div>
  </main>

  <!-- ── TECH STACK MODAL ── -->
  <div class="modal-overlay" id="stack-modal">
    <div class="modal-backdrop" id="stack-modal-backdrop"></div>
    <div class="modal-panel">
      <div class="modal-header">
        <h2 class="modal-title">
          <svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M12 2v2M12 20v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M2 12h2M20 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
          Full Tech Stack
        </h2>
        <button class="modal-close" id="close-stack-modal" aria-label="Close">&times;</button>
      </div>
      <div class="modal-body">
        {Object.entries(techStack).map(([category, tools]) => (
          <div class="modal-stack-group">
            <h3 class="modal-stack-category">{category}</h3>
            <div class="stack-tags">
              {tools.map(tool => (
                <span class="tag">
                  <img src={tool.icon} alt={tool.name} class="tag-icon" width="13" height="13" loading="lazy" />
                  {tool.name}
                </span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- ── EXPERIENCE MODAL ── -->
  <div class="modal-overlay" id="experience-modal">
    <div class="modal-backdrop" id="experience-modal-backdrop"></div>
    <div class="modal-panel modal-panel--narrow">
      <div class="modal-header">
        <h2 class="modal-title">
          <svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="7" width="20" height="14" rx="2"/><path d="M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2"/></svg>
          Full Experience
        </h2>
        <button class="modal-close" id="close-experience-modal" aria-label="Close">&times;</button>
      </div>
      <div class="modal-body">
        <div class="timeline">
          <div class="timeline-item">
            <div class="timeline-dot active"></div>
            <div class="timeline-body">
              <span class="timeline-year">2024 — Present</span>
              <strong>Freelance Full Stack Developer</strong>
              <p>Building and deploying web solutions for local clients. Working on real-world projects end-to-end, from design to deployment.</p>
            </div>
          </div>
          <div class="timeline-item">
            <div class="timeline-dot"></div>
            <div class="timeline-body">
              <span class="timeline-year">2023 — Present</span>
              <strong>IT Student</strong>
              <p>Studying algorithms, system design, and software engineering principles. Applying academic knowledge to real projects.</p>
            </div>
          </div>
          <div class="timeline-item">
            <div class="timeline-dot"></div>
            <div class="timeline-body">
              <span class="timeline-year">2022</span>
              <strong>Started Learning Development</strong>
              <p>Began self-studying web development — HTML, CSS, JavaScript, and basic backend concepts.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ── PROJECTS MODAL ── -->
  <div class="modal-overlay" id="projects-modal">
    <div class="modal-backdrop" id="modal-backdrop"></div>
    <div class="modal-panel">
      <div class="modal-header">
        <h2 class="modal-title">
          <svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7a2 2 0 0 1 2-2h4l2 2h8a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></svg>
          All Projects
        </h2>
        <button class="modal-close" id="close-projects-modal" aria-label="Close">&times;</button>
      </div>
      <div class="modal-body">
        {allProjectsSorted.length > 0 ? (
          <div class="modal-projects-grid">
            {allProjectsSorted.map(project => (
              <a href={`/projects/${project.id}`} class="modal-project-card">
                <span class="project-role">{project.data.role}</span>
                <h3 class="project-title">{project.data.title}</h3>
                <p class="project-desc">{project.data.outcomeSummary}</p>
              </a>
            ))}
          </div>
        ) : (
          <div class="modal-empty">
            <p>No projects yet. Add your projects to the <code>projects</code> content collection.</p>
          </div>
        )}
      </div>
    </div>
  </div>

  <!-- ── SCHEDULE A CALL MODAL ── -->
  <div class="modal-overlay" id="schedule-modal">
    <div class="modal-backdrop" id="schedule-modal-backdrop"></div>
    <div class="modal-panel modal-panel--schedule">
      <div class="modal-header">
        <h2 class="modal-title">
          <svg class="card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
          Schedule a Call
        </h2>
        <button class="modal-close" id="close-schedule-modal" aria-label="Close">&times;</button>
      </div>
      <div class="modal-body">
        <p class="sc-desc">Have a project in mind or want to discuss a collaboration? Book a quick call and we can talk about your idea, goals, and how I can help.</p>

        <div class="sc-layout">
          <!-- LEFT: info -->
          <div class="sc-info">
            <div class="sc-info-block">
              <h3 class="sc-block-title">What we can talk about</h3>
              <ul class="sc-list">
                <li>Your project or product idea</li>
                <li>Development requirements</li>
                <li>Timeline and next steps</li>
              </ul>
            </div>
            <div class="sc-info-block">
              <h3 class="sc-block-title">Call details</h3>
              <ul class="sc-list sc-list--details">
                <li><span class="sc-detail-icon">⏱</span> Duration: 30 minutes</li>
                <li><span class="sc-detail-icon">📹</span> Platform: Google Meet</li>
              </ul>
            </div>
          </div>

          <!-- RIGHT: form -->
          <form class="sc-form" id="schedule-form" novalidate>
            <div class="sc-field">
              <label class="sc-label" for="sc-name">Your Name</label>
              <input class="sc-input" type="text" id="sc-name" placeholder="Juan dela Cruz" required />
            </div>
            <div class="sc-field">
              <label class="sc-label" for="sc-email">Email</label>
              <input class="sc-input" type="email" id="sc-email" placeholder="you@example.com" required />
            </div>
            <div class="sc-field">
              <label class="sc-label" for="sc-details">Project details <span class="sc-optional">(optional)</span></label>
              <textarea class="sc-input sc-textarea" id="sc-details" placeholder="Brief description of your project or what you'd like to discuss..." rows="3"></textarea>
            </div>
            <div class="sc-field">
              <label class="sc-label">📅 Choose Date & Time</label>
              <div class="sc-datetime-row">
                <div class="sc-field">
                  <label class="sc-label sc-label--sub" for="sc-date">Date</label>
                  <input class="sc-input" type="date" id="sc-date" required />
                </div>
                <div class="sc-field">
                  <label class="sc-label sc-label--sub" for="sc-time">Time</label>
                  <input class="sc-input" type="time" id="sc-time" required />
                </div>
              </div>
            </div>
            <button type="submit" class="sc-submit" id="schedule-submit">Schedule Call</button>
            <p class="sc-success" id="sc-success" style="display:none">✅ Request sent! I'll confirm via email within 24 hours.</p>
            <p class="sc-error" id="sc-error" style="display:none">Something went wrong. Email me at <a href="mailto:louinaquines@gmail.com">louinaquines@gmail.com</a></p>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- ── LIGHTBOX ── -->
  <div class="lightbox" id="lightbox">
    <div class="lightbox-backdrop" id="lightbox-backdrop"></div>
    <button class="lightbox-close" id="lightbox-close" aria-label="Close">&times;</button>
    <button class="lightbox-prev" id="lightbox-prev" aria-label="Previous">&#8249;</button>
    <button class="lightbox-next" id="lightbox-next" aria-label="Next">&#8250;</button>
    <div class="lightbox-content">
      <img src="" alt="" class="lightbox-img" id="lightbox-img" />
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11" is:inline></script>
  <ChatBot />

</BaseLayout>

<script>
  document.addEventListener('astro:page-load', () => {

    // ── PAGE LOADER ──
    const loader = document.getElementById('page-loader');
    if (loader) {
      // Always show loader on page load / reload
      loader.classList.remove('loaded');
      document.body.style.overflow = 'hidden';

      // Fill the bar then fade out
      const fill = loader.querySelector('.loader-bar-fill') as HTMLElement | null;
      if (fill) fill.style.width = '0%';

      let progress = 0;
      const interval = setInterval(() => {
        progress += Math.random() * 18 + 8;
        if (progress >= 100) {
          progress = 100;
          clearInterval(interval);
          if (fill) fill.style.width = '100%';
          setTimeout(() => {
            loader.classList.add('loaded');
            document.body.style.overflow = '';
            // Trigger reveals after loader exits
            setTimeout(initReveal, 200);
          }, 350);
        } else {
          if (fill) fill.style.width = progress + '%';
        }
      }, 80);
    } else {
      initReveal();
    }

    // ── SCROLL REVEAL ──
    function initReveal() {
      const reveals = document.querySelectorAll<HTMLElement>('.reveal');
      if (!reveals.length) return;

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            (entry.target as HTMLElement).classList.add('revealed');
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });

      reveals.forEach(el => observer.observe(el));
    }

    // ── SCHEDULE MODAL ──
    const scheduleModal   = document.getElementById('schedule-modal');
    const openScheduleBtn = document.getElementById('open-schedule-modal');
    const closeScheduleBtn= document.getElementById('close-schedule-modal');
    const scheduleBackdrop= document.getElementById('schedule-modal-backdrop');
    const scheduleForm    = document.getElementById('schedule-form') as HTMLFormElement | null;

    // Set min date to today
    const dateInput = document.getElementById('sc-date') as HTMLInputElement | null;
    if (dateInput) {
      dateInput.min = new Date().toISOString().split('T')[0];
    }

    function openScheduleModal()  { scheduleModal?.classList.add('active');    document.body.style.overflow = 'hidden'; }
    function closeScheduleModal() { scheduleModal?.classList.remove('active'); document.body.style.overflow = ''; }
    openScheduleBtn?.addEventListener('click', openScheduleModal);
    closeScheduleBtn?.addEventListener('click', closeScheduleModal);
    scheduleBackdrop?.addEventListener('click', closeScheduleModal);

    scheduleForm?.addEventListener('submit', (e) => {
      e.preventDefault();
      const submitBtn = document.getElementById('schedule-submit') as HTMLButtonElement | null;

      const name    = (document.getElementById('sc-name')    as HTMLInputElement)?.value.trim()    || '';
      const email   = (document.getElementById('sc-email')   as HTMLInputElement)?.value.trim()    || '';
      const details = (document.getElementById('sc-details') as HTMLTextAreaElement)?.value.trim() || 'N/A';
      const date    = (document.getElementById('sc-date')    as HTMLInputElement)?.value           || '';
      const time    = (document.getElementById('sc-time')    as HTMLInputElement)?.value           || '';

      // Validation
      if (!name || !email || !date || !time) {
        (window as any).Swal?.fire({
          icon: 'warning',
          title: 'Missing fields',
          text: 'Please fill in your name, email, date, and time.',
          confirmButtonColor: '#374151',
          background: 'var(--color-bg-secondary)',
          color: 'var(--color-text)',
        });
        return;
      }

      if (submitBtn) { submitBtn.textContent = 'Scheduling...'; submitBtn.disabled = true; }

      // Format readable datetime for email
      const dateObj  = new Date(`${date}T${time}`);
      const readable = dateObj.toLocaleString('en-PH', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' });

      const subject = encodeURIComponent(`Call Request from ${name}`);
      const body    = encodeURIComponent(`Name: ${name}\nEmail: ${email}\nRequested Time: ${readable}\n\nProject Details:\n${details}`);
      window.open(`mailto:louinaquines@gmail.com?subject=${subject}&body=${body}`, '_blank');

      if (submitBtn) { submitBtn.textContent = 'Schedule Call'; submitBtn.disabled = false; }

      // Reset form
      scheduleForm?.reset();

      // Close modal first, then show SweetAlert
      closeScheduleModal();

      setTimeout(() => {
        (window as any).Swal?.fire({
          icon: 'success',
          title: '🗓️ Call Scheduled!',
          html: `
            <p style="margin:0 0 0.5rem;color:var(--color-text-secondary)">Thanks, <strong>${name}</strong>! Your call request has been sent.</p>
            <p style="margin:0;font-size:0.875rem;color:var(--color-text-muted)">📅 ${readable}</p>
            <p style="margin:0.75rem 0 0;font-size:0.85rem;color:var(--color-text-muted)">I'll confirm via email within 24 hours. See you then! 👋</p>
          `,
          confirmButtonText: 'Sounds good!',
          confirmButtonColor: '#374151',
          background: 'var(--color-bg-secondary)',
          color: 'var(--color-text)',
          timer: 8000,
          timerProgressBar: true,
          showClass: { popup: 'animate__animated animate__fadeInDown' },
        });
      }, 300);
    });

    // ── TECH STACK MODAL ──
    const stackModal = document.getElementById('stack-modal');
    const openStackBtn = document.getElementById('open-stack-modal');
    const closeStackBtn = document.getElementById('close-stack-modal');
    const stackBackdrop = document.getElementById('stack-modal-backdrop');
    function openStackModal() { stackModal?.classList.add('active'); document.body.style.overflow = 'hidden'; }
    function closeStackModal() { stackModal?.classList.remove('active'); document.body.style.overflow = ''; }
    openStackBtn?.addEventListener('click', openStackModal);
    closeStackBtn?.addEventListener('click', closeStackModal);
    stackBackdrop?.addEventListener('click', closeStackModal);

    // ── EXPERIENCE MODAL ──
    const expModal = document.getElementById('experience-modal');
    const openExpBtn = document.getElementById('open-experience-modal');
    const closeExpBtn = document.getElementById('close-experience-modal');
    const expBackdrop = document.getElementById('experience-modal-backdrop');
    function openExpModal() { expModal?.classList.add('active'); document.body.style.overflow = 'hidden'; }
    function closeExpModal() { expModal?.classList.remove('active'); document.body.style.overflow = ''; }
    openExpBtn?.addEventListener('click', openExpModal);
    closeExpBtn?.addEventListener('click', closeExpModal);
    expBackdrop?.addEventListener('click', closeExpModal);

    // ── PROJECTS MODAL ──
    const projectsModal = document.getElementById('projects-modal');
    const openBtn = document.getElementById('open-projects-modal');
    const closeBtn = document.getElementById('close-projects-modal');
    const modalBackdrop = document.getElementById('modal-backdrop');

    function openModal() {
      projectsModal?.classList.add('active');
      document.body.style.overflow = 'hidden';
    }
    function closeModal() {
      projectsModal?.classList.remove('active');
      document.body.style.overflow = '';
    }

    openBtn?.addEventListener('click', openModal);
    closeBtn?.addEventListener('click', closeModal);
    modalBackdrop?.addEventListener('click', closeModal);
    document.addEventListener('keydown', e => {
      if (e.key === 'Escape') {
        if (projectsModal?.classList.contains('active')) closeModal();
        if (stackModal?.classList.contains('active')) closeStackModal();
        if (expModal?.classList.contains('active')) closeExpModal();
        if (scheduleModal?.classList.contains('active')) closeScheduleModal();
      }
    });

    // ── LIGHTBOX ──
    const lightbox = document.getElementById('lightbox');
    const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement | null;
    const lbClose = document.getElementById('lightbox-close');
    const prevBtn = document.getElementById('lightbox-prev');
    const nextBtn = document.getElementById('lightbox-next');
    const lbBackdrop = document.getElementById('lightbox-backdrop');

    const galleryItems = Array.from(document.querySelectorAll<HTMLElement>('.gallery-item[data-index]'));
    const images = galleryItems
      .map(item => {
        const img = item.querySelector<HTMLImageElement>('img');
        return img ? { src: img.src, alt: img.alt } : null;
      })
      .filter((x): x is { src: string; alt: string } => x !== null);

    let current = 0;

    function openLightbox(index: number) {
      if (!lightbox || !lightboxImg || !images[index]) return;
      current = index;
      lightboxImg.src = images[index].src;
      lightboxImg.alt = images[index].alt;
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    }
    function closeLightbox() {
      lightbox?.classList.remove('active');
      document.body.style.overflow = '';
    }
    function showPrev() { openLightbox((current - 1 + images.length) % images.length); }
    function showNext() { openLightbox((current + 1) % images.length); }

    galleryItems.forEach(item => {
      item.addEventListener('click', () => {
        const idx = parseInt(item.getAttribute('data-index') || '0');
        if (images[idx]) openLightbox(idx);
      });
    });

    lbClose?.addEventListener('click', closeLightbox);
    lbBackdrop?.addEventListener('click', closeLightbox);
    prevBtn?.addEventListener('click', showPrev);
    nextBtn?.addEventListener('click', showNext);
    document.addEventListener('keydown', e => {
      if (!lightbox?.classList.contains('active')) return;
      if (e.key === 'Escape') closeLightbox();
      if (e.key === 'ArrowLeft') showPrev();
      if (e.key === 'ArrowRight') showNext();
    });

    // ── SCROLL & NAV ──
    const stored = sessionStorage.getItem('scrollTo');
    if (stored) {
      sessionStorage.removeItem('scrollTo');
      setTimeout(() => {
        const target = document.querySelector(stored);
        if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }, 200);
    }
    if (window.location.hash && !stored) {
      const target = document.querySelector(window.location.hash);
      if (target) {
        setTimeout(() => target.scrollIntoView({ behavior: 'smooth', block: 'start' }), 150);
      }
    }
    document.querySelectorAll('a[href^="#"]').forEach(link => {
      link.addEventListener('click', e => {
        const hash = link.getAttribute('href');
        if (!hash || hash === '#') return;
        const target = document.querySelector(hash);
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth', block: 'start' });
          history.pushState(null, '', hash);
        }
      });
    });
  });
</script>

<style>
  .page {
    max-width: 1000px;
    margin: 0 auto;
    padding: 2rem 1.5rem 5rem;
  }

  /* ── PROFILE HEADER ── */
  .profile-header {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 2rem 2.5rem;
    margin-bottom: 1.5rem;
  }
  .profile-header-inner {
    display: flex;
    align-items: center;
    gap: 2rem;
  }
  .avatar {
    width: 90px;
    height: 90px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    border: 2px solid var(--color-border);
  }
  .profile-name-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: -1rem;
    flex-wrap: wrap;
  }
  .profile-name {
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }
  .profile-location {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    margin-bottom: 0.15rem;
  }
  .profile-role {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin-bottom: .5rem;
  }
  .profile-actions { display: flex; gap: 0.75rem; flex-wrap: wrap; }

  /* ── MAIN GRID ── */
  .main-grid {
    display: grid;
    grid-template-columns: 1fr 360px;
    gap: 1.5rem;
    align-items: start;
  }
  .col-left, .col-right {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  /* ── CARD ── */
  .card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 1.75rem 2rem;
  }
  .card > p {
    font-size: 0.9375rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: 0.75rem;
  }
  .card > p:last-child { margin-bottom: 0; }
  .card-title {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 1.25rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .card-icon {
    width: 16px;
    height: 16px;
    flex-shrink: 0;
    color: var(--color-text);
  }
  .card-title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1.25rem;
  }
  .card-title-row .card-title { margin-bottom: 0; }
  .view-all {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    background: none;
    border: none;
    font-weight: 500;
    cursor: pointer;
    transition: color 0.2s;
    padding: 0;
    font-family: inherit;
  }
  .view-all:hover { color: var(--color-text); }
  .card-sub {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin-top: -0.75rem;
    margin-bottom: 1rem;
  }

  /* ── TECH STACK ── */
  .stack-group { margin-bottom: 1.25rem; }
  .stack-group:last-child { margin-bottom: 0; }
  .stack-category {
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
    margin-bottom: 0.6rem;
  }
  .stack-tags { display: flex; flex-wrap: wrap; gap: 0.4rem; }
  .tag {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.8rem;
    padding: 0.3rem 0.65rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    color: var(--color-text-secondary);
  }
  .tag-icon {
    width: 13px;
    height: 13px;
    object-fit: contain;
    flex-shrink: 0;
    display: block;
  }
  [data-theme="dark"] .tag-icon[src*="000000"],
  [data-theme="dark"] .contact-icon img[src*="000000"] {
    filter: invert(1);
  }

  /* ── PROJECTS ── */
  .projects-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }
  .project-card {
    display: block;
    padding: 1rem 1.25rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    text-decoration: none;
    transition: border-color 0.2s;
  }
  .project-card:hover { border-color: var(--color-text-muted); }
  .project-role {
    display: block;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    color: var(--color-text-muted);
    margin-bottom: 0.3rem;
  }
  .project-title {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.35rem;
    line-height: 1.3;
  }
  .project-desc {
    font-size: 0.8125rem;
    color: var(--color-text-secondary);
    line-height: 1.5;
    margin: 0;
  }

  /* ── PROJECTS MODAL ── */
  .modal-overlay {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 900;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    overflow: hidden;
  }
  .modal-overlay.active { display: flex; }
  .modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    cursor: pointer;
  }
  .modal-panel {
    position: relative;
    z-index: 1;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 20px;
    width: 100%;
    max-width: 720px;
    height: auto;
    max-height: calc(100vh - 3rem);
    display: flex;
    flex-direction: column;
    box-shadow: 0 24px 64px rgba(0,0,0,0.25);
    animation: modal-in 0.2s ease;
    overflow: hidden;
  }
  @keyframes modal-in {
    from { opacity: 0; transform: scale(0.96) translateY(8px); }
    to   { opacity: 1; transform: scale(1) translateY(0); }
  }
  .modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.5rem 1.75rem;
    border-bottom: 1px solid var(--color-border);
    flex-shrink: 0;
  }
  .modal-title {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0;
  }
  .modal-close {
    background: none;
    border: none;
    color: var(--color-text-muted);
    font-size: 1.75rem;
    line-height: 1;
    cursor: pointer;
    padding: 0;
    transition: color 0.2s;
  }
  .modal-close:hover { color: var(--color-text); }
  .modal-body {
    padding: 1.5rem 1.75rem;
    overflow-y: auto;
    flex: 1;
    min-height: 0;
    -webkit-overflow-scrolling: touch;
  }
  .modal-projects-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }
  .modal-project-card {
    display: block;
    padding: 1rem 1.25rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    text-decoration: none;
    transition: border-color 0.2s;
  }
  .modal-project-card:hover { border-color: var(--color-text-muted); }
  .modal-empty {
    text-align: center;
    padding: 2rem;
    color: var(--color-text-muted);
    font-size: 0.9375rem;
  }
  .modal-empty code {
    font-family: monospace;
    background: var(--color-bg);
    padding: 0.1rem 0.35rem;
    border-radius: 4px;
  }

  /* ── MODAL STACK ── */
  .modal-stack-group { margin-bottom: 1.5rem; }
  .modal-stack-group:last-child { margin-bottom: 0; }
  .modal-stack-category {
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
    margin-bottom: 0.6rem;
  }
  .modal-panel--narrow { max-width: 520px; }

  /* ── GALLERY ── */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.6rem;
    margin-bottom: 0.75rem;
  }
  .gallery-item {
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid var(--color-border);
    cursor: pointer;
  }
  .gallery-item:hover .gallery-img {
    opacity: 0.8;
    transform: scale(1.03);
  }
  .gallery-img {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    display: block;
    transition: opacity 0.2s, transform 0.2s;
  }
  .gallery-placeholder {
    width: 100%;
    aspect-ratio: 1 / 1;
    background: var(--color-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
  }

  /* ── LIGHTBOX ── */
  .lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    align-items: center;
    justify-content: center;
  }
  .lightbox.active { display: flex; }
  .lightbox-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.92);
    cursor: pointer;
  }
  .lightbox-content {
    position: relative;
    z-index: 1;
    max-width: 90vw;
    max-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .lightbox-img {
    max-width: 90vw;
    max-height: 88vh;
    object-fit: contain;
    border-radius: 8px;
    box-shadow: 0 25px 60px rgba(0,0,0,0.5);
  }
  .lightbox-close {
    position: fixed;
    top: 1.25rem;
    right: 1.5rem;
    background: none;
    border: none;
    color: #fff;
    font-size: 2.5rem;
    line-height: 1;
    cursor: pointer;
    opacity: 0.8;
    transition: opacity 0.2s;
    z-index: 1002;
  }
  .lightbox-close:hover { opacity: 1; }
  .lightbox-prev,
  .lightbox-next {
    position: fixed;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255,255,255,0.1);
    border: none;
    color: #fff;
    font-size: 2.5rem;
    line-height: 1;
    cursor: pointer;
    padding: 0.6rem 1rem;
    border-radius: 8px;
    opacity: 0.7;
    transition: opacity 0.2s, background 0.2s;
    z-index: 1002;
  }
  .lightbox-prev { left: 1rem; }
  .lightbox-next { right: 1rem; }
  .lightbox-prev:hover,
  .lightbox-next:hover { opacity: 1; background: rgba(255,255,255,0.2); }

  /* ── TIMELINE ── */
  .timeline { display: flex; flex-direction: column; }
  .timeline-item {
    display: flex;
    gap: 1rem;
    padding-bottom: 1.25rem;
    position: relative;
  }
  .timeline-item:last-child { padding-bottom: 0; }
  .timeline-item:not(:last-child)::before {
    content: '';
    position: absolute;
    left: 7px;
    top: 16px;
    bottom: 0;
    width: 1px;
    background: var(--color-border);
  }
  .timeline-dot {
    width: 15px;
    height: 15px;
    border-radius: 50%;
    border: 2px solid var(--color-border);
    background: var(--color-bg);
    flex-shrink: 0;
    margin-top: 2px;
    z-index: 1;
  }
  .timeline-dot.active { background: #757575; border-color: #242424; }
  .timeline-year {
    display: block;
    font-size: 0.75rem;
    color: var(--color-text-muted);
    margin-bottom: 0.2rem;
    font-weight: 500;
  }
  .timeline-body strong {
    display: block;
    font-size: 0.9375rem;
    color: var(--color-text);
    margin-bottom: 0.2rem;
  }
  .timeline-body p {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  /* ── GOALS ── */
  .goals-list { display: flex; flex-direction: column; gap: 1rem; }
  .goal-item {
    padding-left: 0.875rem;
    border-left: 2px solid var(--color-border);
  }
  .goal-item strong {
    display: block;
    font-size: 0.9375rem;
    color: var(--color-text);
    margin-bottom: 0.2rem;
  }
  .goal-item p {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  /* ── CONTACT ── */
  .contact-group-title {
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-text-muted);
    margin-bottom: 0.875rem;
  }
  .contact-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
  }
  .contact-list li {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .contact-icon {
    width: 2rem;
    height: 2rem;
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .contact-icon img {
    display: block;
    object-fit: contain;
  }
  .contact-label {
    display: block;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--color-text-muted);
  }
  .contact-value {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color 0.2s;
  }
  .contact-value:hover { color: var(--color-text); }

  /* ── BUTTONS ── */
  .btn-primary {
    display: inline-block;
    background: var(--color-text);
    color: var(--color-bg);
    padding: 0.6rem 1.25rem;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.2s;
  }
  .btn-primary:hover { opacity: 0.8; }
  .btn-secondary {
    display: inline-block;
    border: 1px solid var(--color-border);
    color: var(--color-text);
    padding: 0.6rem 1.25rem;
    border-radius: 8px;
    font-size: 0.875rem;
    font-weight: 600;
    text-decoration: none;
    transition: border-color 0.2s;
  }
  .btn-secondary:hover { border-color: var(--color-text-muted); }

  /* ── SCHEDULE MODAL ── */
  .modal-panel--schedule {
    max-width: 680px;
  }
  .sc-desc {
    font-size: 0.9rem;
    color: var(--color-text-secondary);
    line-height: 1.65;
    margin: 0 0 1.5rem;
  }
  .sc-layout {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 2rem;
    align-items: start;
  }
  .sc-info { display: flex; flex-direction: column; gap: 1.25rem; }
  .sc-info-block {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1rem 1.25rem;
  }
  .sc-block-title {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.07em;
    color: var(--color-text-muted);
    margin: 0 0 0.75rem;
  }
  .sc-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .sc-list li {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    padding-left: 1rem;
    position: relative;
    line-height: 1.4;
  }
  .sc-list:not(.sc-list--details) li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-text-muted);
  }
  .sc-list--details li {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding-left: 0;
  }
  .sc-detail-icon { font-size: 0.9rem; }

  /* Form */
  .sc-form { display: flex; flex-direction: column; gap: 0.875rem; }
  .sc-field { display: flex; flex-direction: column; gap: 0.3rem; }
  .sc-label {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-text);
  }
  .sc-optional {
    font-weight: 400;
    color: var(--color-text-muted);
    font-size: 0.75rem;
  }
  .sc-input {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 0.55rem 0.875rem;
    font-size: 0.875rem;
    color: var(--color-text);
    font-family: inherit;
    outline: none;
    transition: border-color 0.2s;
    width: 100%;
    box-sizing: border-box;
    appearance: none;
    -webkit-appearance: none;
  }
  .sc-input:focus { border-color: var(--color-text-muted); }
  .sc-input::placeholder { color: var(--color-text-muted); opacity: 1; }
  .sc-textarea {
    resize: vertical;
    min-height: 72px;
  }
  .sc-submit {
    margin-top: 0.25rem;
    background: var(--color-text);
    color: var(--color-bg);
    border: none;
    border-radius: 8px;
    padding: 0.65rem 1.25rem;
    font-size: 0.9rem;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    transition: opacity 0.2s;
    width: 100%;
  }
  .sc-submit:hover    { opacity: 0.82; }
  .sc-submit:disabled { opacity: 0.45; cursor: not-allowed; }
  .sc-success {
    display: none;
  }
  .sc-error {
    display: none;
  }
  .sc-error a { color: inherit; }
  .sc-datetime-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }
  .sc-label--sub {
    font-size: 0.72rem;
    font-weight: 500;
    color: var(--color-text-muted);
    margin-bottom: 0.2rem;
  }
  /* btn-schedule-trigger: make it behave like the <a> btn-secondary */
  .btn-schedule-trigger {
    cursor: pointer;
    background: transparent;
    font-family: inherit;
  }

  /* ── PAGE LOADER ── */
  .page-loader {
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: var(--color-bg);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: opacity 0.5s ease, visibility 0.5s ease;
    opacity: 1;
    visibility: visible;
  }
  .page-loader.loaded {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }
  .loader-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }
  .loader-logo {
    font-size: 2rem;
    font-weight: 800;
    letter-spacing: -0.04em;
    color: var(--color-text);
    opacity: 0;
    transform: translateY(12px);
    animation: loaderLogoIn 0.5s cubic-bezier(0.34,1.56,0.64,1) 0.1s forwards;
  }
  @keyframes loaderLogoIn {
    to { opacity: 1; transform: translateY(0); }
  }
  .loader-bar {
    width: 160px;
    height: 2px;
    background: var(--color-border);
    border-radius: 2px;
    overflow: hidden;
    opacity: 0;
    animation: loaderBarIn 0.3s ease 0.35s forwards;
  }
  @keyframes loaderBarIn {
    to { opacity: 1; }
  }
  .loader-bar-fill {
    height: 100%;
    width: 0%;
    background: var(--color-text);
    border-radius: 2px;
    transition: width 0.12s ease;
  }

  /* ── SCROLL REVEAL ── */
  .reveal {
    opacity: 0;
    transition: opacity 0.6s ease, transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .reveal-up    { transform: translateY(28px); }
  .reveal-left  { transform: translateX(20px); }
  .reveal.revealed {
    opacity: 1;
    transform: translate(0, 0);
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 820px) {
    .main-grid { grid-template-columns: 1fr; }
    .profile-header-inner { flex-direction: column; align-items: flex-start; gap: 1.25rem; }
    .projects-grid { grid-template-columns: 1fr; }
    .modal-projects-grid { grid-template-columns: 1fr; }
    .sc-layout { grid-template-columns: 1fr; gap: 1.25rem; }
  }
  @media (max-width: 480px) {
    .page { padding: 1rem 1rem 4rem; }
    .card { padding: 1.25rem; }
    .profile-header { padding: 1.25rem; }
    .gallery-grid { grid-template-columns: repeat(2, 1fr); }
    .profile-actions { flex-direction: column; }
    .btn-primary, .btn-secondary { text-align: center; width: 100%; }
    .modal-panel { max-height: 90vh; }
    .modal-header, .modal-body { padding: 1.25rem; }
  }
</style>